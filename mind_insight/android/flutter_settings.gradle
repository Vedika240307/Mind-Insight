// Generated by Flutter, this file configures the Flutter tool integration.
// If you manually edit it, ensure paths are correct.

def flutterRoot = rootProject.ext.has("flutterRoot")
        ? rootProject.ext.flutterRoot
        : null

if (flutterRoot == null) {
    def localPropertiesFile = new File(rootProject.projectDir.parentFile, "local.properties")
    if (localPropertiesFile.exists()) {
        Properties properties = new Properties()
        localPropertiesFile.withInputStream { stream ->
            properties.load(stream)
        }
        flutterRoot = properties.getProperty("flutter.sdk")
    }
}

if (flutterRoot == null) {
    throw newGradleException("Flutter SDK not found. Define flutter.sdk in local.properties.")
}

apply from: "$flutterRoot/packages/flutter_tools/gradle/app_plugin_loader.gradle"